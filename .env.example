APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
# Dev:  http://localhost:8000  (hoặc http://127.0.0.1:8000 — phải khớp với php artisan serve)
# Prod: https://yourdomain.com  (không có trailing slash, không có port cho 80/443)
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# Cho phép php artisan serve xử lý nhiều request đồng thời (chỉ ảnh hưởng built-in PHP server)
# Production dùng nginx/php-fpm nên setting này không có tác dụng
PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
# SANCTUM_STATEFUL_DOMAINS chỉ cần khi frontend và API khác domain.
# Khi cùng domain, Sanctum tự derive từ APP_URL — không cần set thủ công.

BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="no-reply@yourdomain.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# ── Laravel Reverb (WebSocket) ──────────────────────────────────────────
# Generate keys: php artisan reverb:install
REVERB_APP_ID=
REVERB_APP_KEY=
REVERB_APP_SECRET=
REVERB_HOST="localhost"
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# ── Browser Push (Web Push API) ──────────────────────────────────────────
# Generate keys: php artisan webpush:vapid
VAPID_SUBJECT="mailto:admin@yourdomain.com"
VAPID_PUBLIC_KEY=
# NOTE: VAPID_PRIVATE_KEY must NOT be committed — generate per environment

# ── Cloudflare Turnstile (bot protection trên login form) ───────────────────
# Lấy keys tại: https://dash.cloudflare.com → Turnstile → Add site
# Set cả 2 keys → tự động bật trên production. Bỏ trống → tắt (log warning).
# Test keys (luôn pass, dùng cho staging/CI):
#   TURNSTILE_SITE_KEY=1x00000000000000000000AA
#   TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# Production: MAIL_MAILER=resend + domain verified tại resend.com/domains
# Dev/test: MAIL_MAILER=log  (dùng onboarding@resend.dev để test trước khi verify domain)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxx

# QUEUE_CONNECTION=redis   # hoặc database nếu chưa có Redis

# ── Social OAuth ─────────────────────────────────────────────────────────────
# Google: https://console.cloud.google.com → Credentials → OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=/auth/social/google/callback

# Facebook: https://developers.facebook.com → App → Settings → Basic
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
# FACEBOOK_REDIRECT_URI=/auth/social/facebook/callback

# LinkedIn (OpenID Connect): https://www.linkedin.com/developers/apps
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
# LINKEDIN_REDIRECT_URI=/auth/social/linkedin/callback

SCOUT_DRIVER=meilisearch

MEILISEARCH_HOST=http://127.0.0.1:7700
MEILISEARCH_KEY=VSjFyT82kuF-_U30ffJJw4om3wy-6fgZzE2SXxydla0     # ← dùng đúng key bạn đã set